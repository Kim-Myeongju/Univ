1. print("") //(중첩반복문에서) 내부 반복문이 종료될 때마다 실행되어 다음 줄로 넘어감

2.  >>> a = [A, B, C]
    >>> for i in a:
            print(i)
        //A\nB\nC 출력

3. 반복문 안에 continue가 있으면 //다음 반복을 시작함?? 다음 문장 아님?

4. abs() 함수 //절댓값 함수

5. 함수 정의할 때 'def 함수명(매개변수):'이 줄을 head라고 부름

6. 